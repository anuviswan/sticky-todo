<UserControl x:Class="StickyDo.Widget.StickyNoteTaskItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behaviors="clr-namespace:StickyDo.Widget.Controls.Behaviors"
             mc:Ignorable="d"
             Height="Auto"
             Padding="0,4,0,4">

    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Checkbox (shared style with the Note List preview, see Resources/Styles.xaml) -->
        <CheckBox Grid.Column="0"
                  Style="{StaticResource TaskCheckBoxStyle}"
                  IsChecked="{Binding IsCompleted, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  VerticalAlignment="Top"
                  Margin="0,2,12,0"/>

        <!-- Task Title (Editable RichTextBox) - SingleLine so Enter submits the task (like the
             plain TextBox this replaces) instead of starting a new line. -->
        <RichTextBox Grid.Column="1"
                     behaviors:RichTextEditorBehavior.PlainText="{Binding Title, Mode=TwoWay}"
                     behaviors:RichTextEditorBehavior.Formatting="{Binding TitleFormatting, Mode=TwoWay}"
                     behaviors:RichTextEditorBehavior.SingleLine="True"
                     behaviors:RichTextEditorBehavior.SubmitCommand="{Binding SubmitEditCommand}"
                     FontSize="13"
                     Foreground="#333333"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="0"
                     VerticalAlignment="Top">
            <RichTextBox.Resources>
                <!-- Match the plain TextBox's tight line spacing. -->
                <Style TargetType="Paragraph">
                    <Setter Property="Margin" Value="0"/>
                </Style>
                <!-- Completed tasks show struck-through, dimmed text, same as the plain TextBox
                     this replaces. Only applies to Runs that don't already carry an explicit
                     user-applied decoration (Style setters lose to a Run's own local value), so a
                     completed task's own Bold/Italic/Underline/Strikethrough formatting is kept. -->
                <Style TargetType="Run">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                            <Setter Property="TextDecorations" Value="Strikethrough"/>
                            <Setter Property="Foreground" Value="#999999"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>

        <!-- Delete Button (only visible for unchecked tasks) -->
        <Button Grid.Column="2"
                Width="20"
                Height="20"
                Background="Transparent"
                BorderThickness="0"
                Cursor="Hand"
                Padding="0"
                VerticalAlignment="Top"
                Margin="12,0,0,0"
                Command="{Binding DeleteCommand}"
                ToolTip="Delete task">
            <Button.Style>
                <Style TargetType="Button">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <TextBlock Text="×" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#CCCCCC" FontWeight="Bold"/>
        </Button>
    </Grid>
</UserControl>
