<UserControl x:Class="StickyDo.Widget.ColorPalettePopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="160"
             d:DesignWidth="340"
             Background="Transparent"
             x:Name="root">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Margin="0,0,20,0">
        <!-- White Palette Container with Drop Shadow -->
        <Grid>
            <!-- Drop Shadow Border -->
            <Border Background="White" CornerRadius="8">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="6" ShadowDepth="2" Opacity="0.15" Direction="270"/>
                </Border.Effect>
            </Border>

            <!-- Color Grid: 5 columns x 2 rows for 10 colors -->
            <ItemsControl x:Name="ColorItemsControl" ItemsSource="{Binding AvailableColors}" Margin="10,10,10,8">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Circular Color Button with Selection Indicator -->
                        <Grid Width="44" Height="44" Margin="5" HorizontalAlignment="Center">
                            <!-- Color Button (Circular) -->
                            <Button x:Name="ColorButton"
                                    Cursor="Hand"
                                    Command="{Binding DataContext.SelectColorCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding .}"
                                    Tag="{Binding .}"
                                    ToolTip="{Binding Converter={StaticResource ColorNameConverter}}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{Binding ., Converter={StaticResource ColorArgbToBrushConverter}}"/>
                                        <Setter Property="BorderThickness" Value="2"/>
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="22">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="0.85"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- Selection Indicator: Blue Border + Checkmark -->
                            <Border x:Name="SelectionIndicator"
                                    BorderBrush="#2563EB"
                                    BorderThickness="2"
                                    CornerRadius="22"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Width="44"
                                    Height="44"
                                    Visibility="Collapsed">
                                <TextBlock x:Name="Checkmark"
                                           Text="✓"
                                           FontSize="22"
                                           FontWeight="Bold"
                                           Foreground="White"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Effect="{StaticResource CheckmarkShadow}"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <!-- Callout Pointer Arrow (Below Palette, pointing down to palette icon) -->
        <Canvas Height="8" Width="16" HorizontalAlignment="Right" Margin="0,0,8,0">
            <Polygon Points="0,0 8,8 16,0" Fill="White" Stroke="#D0D0D0" StrokeThickness="1" StrokeLineJoin="Round"/>
        </Canvas>
    </StackPanel>
</UserControl>
